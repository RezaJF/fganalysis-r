% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connections.R
\name{create_mock_connection}
\alias{create_mock_connection}
\title{Create a mock fg_data_connection object for testing}
\usage{
create_mock_connection(
  pheno_data,
  labs_data,
  minimum_data = NULL,
  cov_pheno_data = NULL,
  endpoint_data = NULL,
  vnr_data = NULL,
  long_anthropometric_data = NULL
)
}
\arguments{
\item{pheno_data}{Data frame containing phenotype data (required)}

\item{labs_data}{Data frame containing lab measurement data (required)}

\item{minimum_data}{Optional data frame containing minimum phenotype data}

\item{cov_pheno_data}{Optional data frame containing covariate phenotype data}

\item{endpoint_data}{Optional data frame containing endpoint data}

\item{vnr_data}{Optional data frame containing VNR data}

\item{long_anthropometric_data}{Optional data frame containing longitudinal anthropometric data}
}
\value{
A fg_data_connection object that can be used with package functions
}
\description{
Creates a mock connection object using data frames instead of database connections.
This is useful for testing, development, and when working with small datasets that can fit in memory.
}
\examples{

# Create minimal mock connection
mock_conn <- create_mock_connection(
  pheno_data = data.frame(FINNGENID = "FG1", EVENT_AGE = 50, CODE1 = "A01", SOURCE = "PURCH"),
  labs_data = data.frame(FINNGENID = "FG1", EVENT_AGE = 49, VALUE = 100, OMOP_CONCEPT_ID = "L1")
)

# Create comprehensive mock connection with additional data
pheno_data <- data.frame(FINNGENID = c("FG1", "FG2"), EVENT_AGE = c(50, 60),
                        CODE1 = c("A01", "A02"), SOURCE = c("PURCH", "PURCH"))
labs_data <- data.frame(FINNGENID = c("FG1", "FG2"), EVENT_AGE = c(49, 59),
                       VALUE = c(100, 110), OMOP_CONCEPT_ID = c("L1", "L1"))
minimum_data <- data.frame(FINNGENID = c("FG1", "FG2"), SEX = c(1, 2))
cov_data <- data.frame(FINNGENID = c("FG1", "FG2"), AGE_AT_DEATH = c(80, 85))

mock_conn_full <- create_mock_connection(
  pheno_data = pheno_data,
  labs_data = labs_data,
  minimum_data = minimum_data,
  cov_pheno_data = cov_data
)
}
